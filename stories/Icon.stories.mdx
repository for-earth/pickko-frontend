import { createElement } from "react";

import { Meta, Title, IconGallery, IconItem } from "@storybook/blocks";

import { OutlineIcon, SolidIcon } from "lib/assets/icons";

<Meta title="Token/Iconography" />

# Iconography

<br />

## Outline Icon

<IconGallery>
  {Object.entries(OutlineIcon).map(([key, value]) => (
    <IconItem name={key} key={key}>
      {createElement(value, {
        name: key,
      })}
    </IconItem>
  ))}
</IconGallery>

## Solid Icon

<IconGallery>
  {Object.entries(SolidIcon).map(([key, value]) => (
    <IconItem name={key} key={key}>
      {createElement(value, {
        name: key,
      })}
    </IconItem>
  ))}
</IconGallery>
